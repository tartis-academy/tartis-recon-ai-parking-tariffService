# Postgres dedicado de tariff-service (database-per-service, perfil prod o
# servicio aislado). El Postgres compartido del dia a dia en dev vive en el
# repo de infra (./setup.sh). Ver Convenciones del proyecto Parking S4.
#
#   docker network create parking-shared   (una vez)

services:
  postgres:
    image: postgres:15.18-alpine
    container_name: parking-tariff-postgres
    restart: unless-stopped
    environment:
      POSTGRES_USER: ${TARIFF_DB_USER}
      POSTGRES_PASSWORD: ${TARIFF_DB_PASSWORD}
      POSTGRES_DB: ${TARIFF_DB_NAME}
    ports:
      - "${TARIFF_DB_PORT:-5435}:5432"
    volumes:
      - parking-tariff-postgres-data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${TARIFF_DB_USER} -d ${TARIFF_DB_NAME}"]
      interval: 5s
      timeout: 3s
      retries: 5
    networks:
      - parking-shared

volumes:
  parking-tariff-postgres-data:

networks:
  parking-shared:
    external: true
