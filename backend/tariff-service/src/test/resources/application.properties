spring.datasource.url=jdbc:postgresql://${TARIFF_DB_HOST:localhost}:${TARIFF_DB_PORT:5435}/${TARIFF_DB_NAME:tariff_db}
spring.datasource.username=${TARIFF_DB_USER:tariff_user}
spring.datasource.password=${TARIFF_DB_PASSWORD:change.me}

spring.jpa.hibernate.ddl-auto=create-drop
